import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    iconSquareButton: {
        borderRadius: 5,
        borderWidth: 1,
        padding: 8,
        justifyContent: 'center',
        alignItems: 'center',
    }
})