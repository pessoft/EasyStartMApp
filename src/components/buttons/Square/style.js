import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    iconSquareButton: {
        borderRadius: 4,
        borderWidth: 0.5,
        padding: 6,
        justifyContent: 'center',
        alignItems: 'center',
    }
})