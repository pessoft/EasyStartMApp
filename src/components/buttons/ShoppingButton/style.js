import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  shoppingButton: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'flex-end',
    alignItems: 'flex-end'
  }
})