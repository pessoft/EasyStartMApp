import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    iconSquareButton: {
        borderRadius: 5,
        borderWidth: 1,
        padding: 5,
        justifyContent: 'center',
        alignItems: 'center',
    }
})