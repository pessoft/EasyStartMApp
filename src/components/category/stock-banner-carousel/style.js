import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  bannerContainer: {
    flex: 1,
    maxHeight: 110,
    justifyContent: 'flex-start',
    padding: 0,
    margin: 0
  },
})