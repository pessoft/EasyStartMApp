import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  badge: {
    position: 'absolute',
    right: 12,
    top: 0,
    borderRadius: 5,
    paddingHorizontal: 8,
    paddingVertical: 0.8,
  }
})