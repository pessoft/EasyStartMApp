import React from 'react'
import {connect} from 'react-redux'

class ProductReviewScreen extends React.Component {

    render() {
        return (
            <React.Fragment>
            </React.Fragment>
        )
    }
}

export default connect()(ProductReviewScreen)