import React from 'react'
import {connect} from 'react-redux'

class ProductInfoScreen extends React.Component {

    render() {
        return (
            <React.Fragment>
            </React.Fragment>
        )
    }
}

export default connect()(ProductInfoScreen)