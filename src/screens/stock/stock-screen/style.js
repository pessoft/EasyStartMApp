import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  centerScreen: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },

  textInfo: {
    marginTop: 15,
    alignItems: 'center'
  }
})