import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  stockDescription: {
    marginTop: 10,
    alignItems: 'center',
    paddingHorizontal: 15,
    paddingVertical: 10
  }
})