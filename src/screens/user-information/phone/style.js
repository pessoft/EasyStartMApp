import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  screen: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  inputPhoneNumber: {
    textAlign: 'center',
    marginBottom: 15
  },
  inputSize: {
    width: 230,
  }
})